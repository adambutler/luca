<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl mb-5">New activity</h1>

  <%= render "search" %>
  <%= render "results", results: @results, workout: @workout, activity: @activity %>
</div>
